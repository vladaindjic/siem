#spring.application.name=Mongo DB Example


#Mongo Config
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=log-mongo
spring.data.mongodb.username=siem
spring.data.mongodb.password=siem_center123
spring.data.mongodb.repositories.enabled=true

server.port=8443

# Tell Spring Security (if used) to require requests over HTTPS
security.require-ssl=true
# The format used for the keystore
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:jebi_se.p12
# The password used to generate the certificate
server.ssl.key-store-password=siem
